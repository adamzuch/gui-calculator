# gui-calculator

## Running locally

Before these steps ensure Node.js and npm are installed.

1. `npm install`
2. `npm run dev`
3. Go to http://localhost:5173/ in your browser.

## Running tests

Before these steps ensure Node.js and npm are installed.

1. `npm install`
2. `npm run test`
